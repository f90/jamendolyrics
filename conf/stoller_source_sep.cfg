[main]
LABEL_PATH = annotations/words # Path to folder containing the word onset label files
LABEL_EXT = .csv # Extension of the word onset files that should be searched for
MP3_PATH = mp3 # Path to the mp3 files
PREDICTION_PATH = predictions/stoller_sep_model # Path to the prediction files
PREDICTION_EXT = _align.csv # Extension of prediction result filenames
PREDICTION_DELIM = , # Delimiter to use for reading the predicted alignment from the csv files
DELAY = 0.180 # Optional: Change the models predictions to all be later (> 0) or earlier (< 0) to account for a constant prediction delay (number given in seconds)